using UnityEngine;
using System.Collections;

public class Follow : MonoBehaviour {

    public Transform toFollow;
	
	// Update is called once per frame
	void Update () {
       //if (toFollow!= null) transform.position = new Vector3(toFollow.position.x, toFollow.position.y, -10f);
	}
}
